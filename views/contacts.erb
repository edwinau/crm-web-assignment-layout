<h1 id="happy">There are <span><%=Contact.all.count%></span> contacts</h1>

<ul id=wrap>
<% Contact.all.each do |contact| %>
  <li><%= contact.first_name %> <%= contact.last_name %></li>
<% end %>
</ul>
